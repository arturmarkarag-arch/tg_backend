'use strict';

// Backwards-compatible BaseLinker import path. The workflow itself is provider-neutral
// and is shared with Allegro from Stage 5 onward.
module.exports = require('./warehousePickingState');
